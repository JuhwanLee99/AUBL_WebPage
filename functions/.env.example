# App Check 클라이언트를 먼저 배포하고 Firebase Console의 요청 지표를 확인한 뒤 true로 전환합니다.
# 민감 callable(status/submit/admin/집계)을 먼저 강제하고 검증 후 공개 조회를 별도 전환합니다.
ALLSTAR_ENFORCE_APP_CHECK_SENSITIVE=false
ALLSTAR_ENFORCE_APP_CHECK_PUBLIC=false
