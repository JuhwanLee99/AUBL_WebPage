# UniquePlay 수동 동기화 구현·배포 이력

이 문서는 2026 AUBL 시즌의 UniquePlay 데이터 수집, 검수, 게시 기능을 구축하면서 확인된 장애와 수정 이력을 운영 평가·보고서 작성에 재사용할 수 있도록 정리한다. 인증 토큰, 암호화 키, 세션 원문, 데이터베이스 비밀번호 등 비밀 정보는 기록하지 않는다.

## 1. 목표와 운영 원칙

- AUBL 관리자가 명시적으로 `수집 시작` 버튼을 눌렀을 때만 UniquePlay 데이터를 수집한다.
- 스케줄러와 주기적 자동 수집은 사용하지 않는다.
- 수집 결과는 곧바로 공개하지 않고 `수집 → 변경 비교 → 충돌 해결 → 검증 → 리비전 생성 → 활성화` 순서로 처리한다.
- 후보 데이터에 차단 오류가 있거나 체크섬이 달라지면 현재 공개 리비전을 유지한다.
- UniquePlay 개인 계정의 비밀번호를 저장하지 않는다. 수집 워커에는 UniquePlay 도메인 쿠키와 허용된 인증 저장소 항목만 암호화해 전달한다.
- Google 계정 쿠키, 원본 HTML, 전화번호, 이메일과 같은 개인정보는 후보 데이터나 로그에 남기지 않는다.

## 2. 구성

| 구성 요소 | 역할 | 배포 위치 |
| --- | --- | --- |
| React 관리자 화면 | 수동 실행, 진행률, 변경 비교, 매핑, 검증, 게시·활성화 제어 | AUBL 웹 |
| Spring 백엔드 | 실행 원장, 후보·체크섬, 검증, 감사 리비전, MariaDB·Firestore 게시 | NAS 컨테이너 |
| Playwright 수집 워커 | 암호화 세션 복원, A~H조·일정·결과·개인 기록 수집, 허용 목록 정제 | NAS 컨테이너 |
| MariaDB | UniquePlay 시즌 데이터와 동기화 감사 원장 | NAS |
| Firestore | 공개 일정·결과 projection | Firebase |

백엔드 작업본은 웹 저장소와 분리된 `AUBL_WebPage_BE-test` 디렉터리에서 빌드했다. 해당 디렉터리는 작업 당시 Git 저장소가 아니므로 아래 이미지 버전과 이 문서가 배포 추적 기준이다.
v22와 v23의 재현 가능한 소스 변경은 각각 `docs/release/aubl-backend-v22-mapping-persistence.patch`, `docs/release/aubl-backend-v23-season-bootstrap.patch`에 함께 보관한다.

## 3. 이미지 버전별 변경점

### 2026-09-05 스크롤 조기 종료·불완전 후보 차단 — 워커 v11

- v10 실행 `0093edba-f02e-4f64-aee1-bd781d666b95`에서 오늘 경기는 읽었으나 결과 목록 첫 묶음의 정규 경기 6개만 수집해 40팀 경기 수 불일치와 경기/상세 각 112개 삭제 후보가 발생했다. 실제 삭제·공개 변경은 없었다.
- 스크롤 불가능한 192/214px `overflow:visible` 카드를 목록으로 오인한 원인을 확인했다. v11은 표·경기·상세 탐색에 실제 세로 스크롤 영역과 끝 위치를 사용하며, 지연 로딩 안정화를 대기 후 확인한다.
- 조별 순위표 대비 종료 경기 수가 부족하면 상세 수집·후보 생성 전에 `COLLECTION_INCOMPLETE`로 차단한다. 일정 탭 값이나 중복/예정 경기를 누락 보충으로 사용하지 않는다. 기존 검증·매핑·게시 정책은 유지한다.
- 워커 60개, Chromium CSS 재현 1개, 웹 회귀 71개(총 132개) 통과. 자동 수집·게시·활성화는 추가하지 않는다.
- 원인·버전별 수정·테스트·배포 및 롤백 근거는 [v11 운영 기록](release/uniqueplay-scroll-completeness-v11-2026-09-05.md)에 보관한다.

### 2026-09-05 공식 상세 기본값·홈 히어로 통합 — 웹 운영 배포 완료

- 공식 동기화 데이터의 기본 보기를 상세 기록으로 고정했다. 이전 보기 저장값을 무시하고 새로고침·경기 이동 때 초기화한다. 현재 화면에서 문자중계 스타일로 전환하는 기능은 유지한다.
- 같은 경기의 직접 기록 시작/이벤트 증거가 있어야 공식 진행 경기의 Firestore 실시간 기본 경로를 사용한다. 단순 진행 상태, 다른 경기의 로그, 사후 수동 입력을 근거로 선택하지 않는다. 게시된 공식 데이터의 우선순위와 보관 수기 기록은 유지한다.
- 홈 히어로의 연합회교/FORMAT/VISION 카드와 지정한 디지털화 안내 문장을 제거하고 기존 데이터 확인 현황을 그 자리에 통합했다. 문구와 배치 변경은 화면에만 적용하며 원격 운영 콘텐츠를 삭제하지 않는다.
- 상세/문자중계 38개, 히어로 8개 테스트와 기존 UniquePlay 회귀, lint/typecheck/build를 통과했다. Firebase Hosting live 대상과 이전 버전을 확인했다.
- [변경점·검증·배포/롤백 기록](release/web-record-views-hero-2026-09-05.md)에 배포 결과를 추적한다. 백엔드·워커·Flutter 배포와 수집·데이터 게시/활성화는 이번 웹 배포 범위에서 제외한다.
- `a279e55` 검증 빌드를 16:15:35 KST에 `aubl-backup` Hosting live로 배포했다(version `8dfbfa8f6fb197d9`). 실제 `aubl.club`의 JS/CSS 해시가 빌드와 일치하며 홈 패널 통합·두 공식 경기 상세 기본값·토글 후 새로고침을 확인했다. 확인한 브라우저/공개 API 오류 0건, 360px 가로 넘침 없음. 배포 전 버전 `ef3d0dd140d2dfc2`를 롤백 기준으로 기록했다.

### 2026-09-05 문자중계 타순 연결·타자일순 수정 — 웹 로컬 변경

- 원인: 이닝마다 원본 타자 행을 1~9번 순서로 출력하고 `2직,좌안`처럼 같은 선수의 반복 타석을 한 항목으로 표시해 공격 순서가 끊겼다. 수집·게시 데이터 자체를 변경하지 않고 공개 표시 계층을 수정했다.
- `officialBattingFlow`는 타석 종료 결과와 주루·교체 문구를 분류하고, 이전 이닝의 다음 타순부터 1~9번 큐를 순환한다. 같은 이닝의 반복 결과는 별도 타석으로 표시한다. 도루·도루자·폭투·송구실책 등은 별도 타석이 아니라 발생 시점 미상의 원본 문맥으로 보존한다. 투구별 중계·득점·타점·아웃카운트는 재계산하지 않는다.
- 확인 경기: 2026-08-24 경희대 BRAVES 5 : 고려대 백구회 10, `up-f1e054d7678bf5467580de7d`. 고려대 1회 `1·2·3`, 2회 `4·5·6·7·8·9·1·2·3·4·5·6·7·8`, 3회 `9·1·2·3`, 4회 `4·5·6·7`, 5회 `8·9·1·2·3`을 fixture와 실제 로컬 화면의 공개 GET 응답(리비전 `375df8a9-5f3b-49f6-81a6-cc26906c4465`)으로 대조했다. 경희대는 독립된 다음 타순을 유지한다. 브라우저 런타임 오류 0건.
- 타순/이닝 누락, 미분류 결과, 같은 이닝 같은 타순의 교체 선수 순서 모호성, 다음 타자 부재는 강제로 정렬하지 않는다. 해당 이닝과 이후 연결이 불명확한 이닝은 `타순 확인 필요`와 사유를 표시하고 원문으로 전환한다. CSV·타자/투수 수치·Firestore 수기 기록·공개 리비전은 그대로 보존한다.
- 기존 검수 JSON(`f994f85b-b6dc-4845-8b47-dd62eb63f352`)의 상세 보유 112경기/224팀을 읽기 전용 점검했다. 팀별 1,195개 이닝 중 1,146개는 타순 연결 가능, 49개는 모호성/누락 및 그 이후 연결 불명확으로 원문 표시 대상이다. 이는 **표시 재구성 가능성** 점검이며 경기 기록 정확성이나 신규 게시 여부에 대한 판정이 아니다.
- 검증: lint·TypeScript·프로덕션 빌드, 기존 UniquePlay 회귀, 공식 보기 테스트 37개(렌더링/순서/불변성 15개 + 브라우저 22개) 통과. 기존 번들 크기 경고는 유지된다. 백엔드/워커 재빌드, 재수집, 게시, 호스팅 배포는 하지 않았다.

### 2026-09-05 공개 경기 상세 두 가지 보기 — 웹 로컬 변경

- 운영 웹에서 점수만 보이는 현상을 읽기 전용으로 대조했다. 15:34 KST의 공개 API는 리비전 `375df8a9-5f3b-49f6-81a6-cc26906c4465`(15:29 게시)의 상세를 정상 반환했다. 로컬 경기 `up-f1e054d7678bf5467580de7d`는 양 팀 이닝·타자 19행·투수 5행이 표시되지만 운영 웹은 아직 이전 Firestore 문자중계 UI였다. 앞선 v25 배포 기록의 미게시 상태와 이후 확인된 공개 상태를 구분한다. 이 진단/화면 작업에서 수집·게시·활성화는 실행하지 않았다.
- 사용자 요청에 따라 `상세 기록 / 문자중계 스타일` 토글을 추가했다. 최근 선택을 브라우저에 저장하고 두 화면에서 동일한 활성 공식 기록과 품질 표시를 사용한다.
- 최초 별도 레이아웃은 “운영 문자중계 화면 그대로”라는 후속 요청과 달라 제거했다. 운영 화면의 `ScoreboardPanel` 표시부, `NowPlayingCard`, `PostGameSummary`, `LiveFeed`, `CsvRecordPreview`, 공통 `StatsTable`/`RemovedPlayersPanel`을 재사용한다. 기존 실시간 데이터 연결·계산은 유지하고 공식 응답은 별도 읽기 전용 표시 변환을 거친다.
- 기존 화면의 전광판·현재 타석 카드·오른쪽 요약/문자중계·하단 기록표/기록지 배치를 유지했다. 실시간 볼카운트·현재 타자·투구수·교체 여부와 미수집 통계는 0을 만들지 않고 미제공/—로 표시한다. 최초 이닝별 원문 타순 정렬의 한계는 위의 후속 타순 연결 수정에서 보완했다. 기록지 CSV는 공개 값만 담고 수식 주입을 방어한다.
- 두 버전 모두 기록원 상태를 변경하지 않고 기존 수동 기록과 합산하지 않는다. 백엔드·워커·Flutter 소스와 운영 데이터는 변경하지 않았다.
- 사용자가 지정한 경기 `up-15656847f2ed41fb6595ddad`의 로컬 실제 화면도 확인했다. 기존 전광판·현재 타석 카드·기록지 각 1개, 타자/투수 기록 24행이 표시되고 브라우저 런타임 오류는 없었다. 운영 원본을 재수집하거나 기록을 쓰지 않고 활성 공개 응답을 조회했다.
- 다크모드에서 표 머리글·선택 버튼을 의미 기반 배경/전경 토큰으로 수정했다. 토글/이닝 버튼의 48px 터치 영역, 키보드 포커스, KST 시각, 저장소 실패 fallback을 적용했다.
- 공개 API/라우팅/워커 기존 회귀 테스트와 웹 lint/typecheck/build 통과. 렌더링/변환/기존 실시간 회귀 9개와 브라우저 22개(360·390·768·1280·1920px, 라이트·다크, 두 보기, 키보드·다시보기·기록지 탭·전광판 확대·선택 저장·가로 넘침·대비), 총 31개를 검증했다. 새 공식 컨테이너에서 기존 모바일 우측 8px 보정으로 생기는 넘침만 범위를 한정해 제거했다. 기존 번들 크기 경고는 남아 있다. 공개 웹 호스팅 배포는 이번 변경에 포함하지 않았다.

### 2026-09-05 상세 기록 품질과 수동 정정 — 백엔드 v25 / 워커 v10

- 치명적이지 않은 경기 상세 산술 오류를 게시 가능한 경고로 분리했다. 팀·경기 식별, 형식, 개인정보, 종료 경기 커버리지, 원천 점수와 부모 경기 불일치는 계속 차단한다.
- 관리자 검수에 117경기 검색·전체 표·원본 비교·필드별 정정·사유·충돌 결정을 추가했다. 검수 JSON 다운로드는 공개 경기 필드만 포함하며 관리자 메모·행위자·인증 정보는 제외한다.
- 원본 후보를 불변으로 보존하고 수동 정정을 별도 항목으로 기록한다. 원천 값이 정정값과 같아지면 SOURCE_RESOLVED, 다르게 바뀌면 충돌로 남긴다. 상세 미게시·경기 누락은 해결로 간주하지 않는다.
- 공개 경기 상세, 선수 경기 로그와 Flutter에 `CORRECTION_PENDING` / `RESOLVED` 표시를 추가했다. 기존 품질 필드가 없는 자료를 정상 또는 해결로 추정하지 않는다.
- 원본 체크섬, 실제 검수 체크섬, 활성 리비전의 동시성 검사를 적용했다. 상세 경고 게시에는 명시적 확인이 필요하며 새 리비전 생성과 활성화는 자동 실행하지 않는다.
- 워커 v10은 표의 마지막 스크롤 위치와 3회 안정화를 함께 확인한다. 최대 반복 수 도달은 `COLLECTION_INCOMPLETE`로 실패 처리한다. 팀 별칭과 규정 IN/OUT을 포함한 기존 누적 비교 키 문제는 아직 남아 있다.
- 사용자 업로드 승인 후 `synapse9983/aubl-backend:v25`, `synapse9983/aubl-uniqueplay-sync-worker:v10`을 Docker Hub에 업로드하고 NAS 컨테이너를 교체했다. 백엔드 환경 변수 21개, 워커 12개와 기존 연결을 보존했다. schema 6 유지, 신규 migration 없음, 두 컨테이너 정상 기동을 확인했다.
- 기존 v9 수집 실행을 읽기 전용으로 전수 검사했다. 117경기 중 상세 112, 미게시 5, 경고 46경기·80건, 검사 경고 없음 66경기다. 타점 7경기는 UniquePlay 원본과 추가 대조했고 모두 같은 값이었다. 홈런에 타점·득점 0인 2경기는 우선 확인 대상으로 남겼다.
- 백엔드 106·워커/웹 120·Flutter 143개, 총 369개 테스트가 통과했다. 웹 lint/typecheck/build 통과. 번들 크기 경고는 기존과 같이 남아 있다. 운영 기록의 수정·게시·복구를 실제 실행한 것으로 해석하지 않는다.
- [버전별 배포와 검수 기록](release/uniqueplay-record-quality-v25-v10-2026-09-05.md), [전체 117경기 검수 데이터](reports/uniqueplay-game-audit-2026-09-05.json), [Word 보고서](reports/AUBL_2026_경기기록_전수검수_보고서_2026-09-05.docx)를 함께 보관한다. 별도 백엔드 폴더의 v24 대비 재현 패치는 `docs/release/aubl-backend-record-quality-v25-2026-09-05.patch`다.
- 신규 수집·실제 기록 정정·공개 리비전 게시/활성화·웹 호스팅·앱 스토어 배포·Git 원격 push는 하지 않았다. 419개 삭제 후보는 원인별 재대조가 필요하므로 현재 후보의 게시 보류를 유지한다.

### 2026-09-05 경기별 상세 확장 — 백엔드 v24 / 워커 v6·v7·v8·v9

- 확정된 Flutter 디자인 B안을 `getResult`로 fast-forward 통합했다. 비교용 A·B 브랜치를 삭제하지 않았다.
- 워커 어댑터 `2026.09.05.6`에 양 팀 박스스코어·경기별 타자·투수 기록과 이닝별 결과 문자열 수집을 추가했다. 관리자 버튼으로 시작하는 기존 파이프라인을 유지한다.
- 실제 정상 경기와 몰수 경기의 공개 DOM을 확인하여 교체 행, 괄호 안 공백, `지타`·`미정`, 합계 행, 1.2이닝=5아웃, 몰수 합계-only 표를 구분했다. 새로운 원본 HTML이나 세션 파일은 저장하지 않았다.
- 백엔드에 상세 스냅샷 검증, `GAME_RECORD` 비교·검수, 이전 공식 상세 유지, 공식 경기·선수 로그 공개 API를 추가했다. 별도 수동 로그 테이블·Firestore 경기 원장·시즌 누적 기록을 덮어쓰거나 중복 합산하지 않는다.
- 구 워커의 상세 필드 누락, 점수·상태 오버라이드 충돌, 모호한 선수 연결, 잘못된 게시 체크섬을 차단한다. 활성 이후 결과가 달라지면 `REVIEW_REQUIRED`로 표시한다.
- 웹은 외부 경기 ID와 Firestore 문서 ID를 구분하고, 다른 경기로의 fallback과 이전 요청 응답 노출을 방지한다. 앱 선수 상세는 공식 경기별 네이티브 카드를 사용한다.
- 변경된 실제 백엔드 소스는 별도 `AUBL_WebPage_BE-test`에 반영했다. 전체 변경을 `docs/release/aubl-backend-game-details-2026-09-05.patch`에 보관한다.
- Java 21 백엔드 테스트 81개 및 실행 JAR 빌드, 워커 테스트 35개, Flutter 테스트 134개·정적 분석을 통과했다. 웹 API fixture 7개와 경로 회귀 6개를 추가했다.
- 백엔드 v24와 워커 v6을 `linux/amd64`로 빌드·Docker Hub 업로드·NAS 교체했다. 기존 v23/v24 JAR의 V1~V6 마이그레이션 SHA256이 동일하며, 교체 후 Flyway도 schema 6·추가 migration 없음을 확인했다.
- 실제 첫 미리보기 `eac881a1-1dc7-47db-b003-1cd776ff8896`는 `GAME_DETAIL_SCHEMA`로 중단되어 후보·리비전을 만들지 않았다. 공개 원천 대조에서 타자 포지션 `투수`를 투수 표 제목과 혼동하는 결함을 확인했다.
- 워커 v7(`d5f3409`)은 제목 텍스트뿐 아니라 고정 첫 행·2열·통계 헤더로 표를 식별한다. 최대 15초 polling budget·정상 동일 표 2회 확인·안전한 단계와 공개 경기 ID 진단을 추가했다. 관리자 UI에는 진단 코드(`ad53c81`)와 처리 경기 수를 표시한다.
- v7 미리보기 `964e3353-910d-47e3-8c35-20c6a85025ef`의 진단에서 경기 `56556` 최초 표 추출 실패를 좁혔다. 원천의 직접 Text node에 있던 타순·선수명이 leaf-only 추출에서 사라지고 포지션·등번호만 남는 혼합 DOM 결함을 확인했다.
- 워커 v8(`616c3c9`)은 식별된 공개 셀 전체의 `textContent`를 정규화해 직접 텍스트와 자식 span을 함께 보존한다. 원본 HTML·계정 UI 수집은 추가하지 않았고 기존 무결성 검사는 유지했다.
- v8까지 워커 41개·웹 API 9개·경로 6개, 변경 없는 백엔드 81개·Flutter 134개를 포함해 검증 기준선은 271개다. 웹 lint/typecheck/build도 통과했다.
- v8의 실제 수집은 첫 경기 `56556`을 통과하고 `56557`에서 중단됐다. 해당 경기의 선택 정보 행 때문에 탭 위치가 달라지고 등번호 `()`가 존재하는 것을 확인했다. v9(`d55a950`)는 양팀명·선택 스타일을 기준으로 유일한 탭을 찾고 빈 등번호를 null로 보존한다. 워커 43개 및 웹 API/경로 15개가 통과해 전체 기준선은 273개다.
- v9 실제 미리보기 `f994f85b-b6dc-4845-8b47-dd62eb63f352`는 종료 경기 상세 117건을 수집했다. 후보 총 1,802건, 신규 537·변경 8·삭제 후보 419·동일 838·충돌/미해결 0이다. 명시적 서버 재검증에서도 개인 득점 합계 차이 9건과 전 이닝 빈칸 1건으로 게시가 차단됐다. 원천 박스스코어 10경기를 직접 대조했고 수치를 임의 보정하지 않았다. A·F·G조 동률 경고도 남아 있다.
- 관리자 비교표에 읽기 전용 경기별 R/H·선수 합계·이닝 합계, 불일치 항목과 원천 링크를 추가했다(`19032f7`). 빈칸/null/X와 실제 0을 구분하고, 미게시 상세를 합계 정상으로 표시하지 않는다. 서버 검증의 안전한 후보 경로도 노출한다.
- 최종 웹·워커 테스트 72개(워커 43·경로 6·검수 helper 12·API 11)와 전체 lint/typecheck/build를 통과했다. 변경 없는 백엔드 81·Flutter 134개를 합친 기준선은 287개다. 운영 원천 대조와 fixture 통과를 구분하며, 검수 오류를 테스트 성공으로 대체하지 않는다.
- 419개 삭제 후보가 실제 삭제된 선수 수라는 의미는 아니다. 기존 누적 기록 비교 키의 별칭·규정 IN/OUT 문제와 스크롤 조기 종료 재현은 [별도 후속 검토](release/uniqueplay-cumulative-review-followups-2026-09-05.md)에 기록했다. 게시 전에 보완·재대조해야 하며 실제 기록 삭제·새 리비전 활성화는 수행하지 않았다.
- 배포별 digest, 운영 설정 보존, 실행 시각과 검수 결과는 [v24 이후 운영 보고서](release/uniqueplay-game-details-v24-v6-2026-09-05.md)에 보관한다. 새 상세 리비전 생성·활성화, Git 원격 push, 공개 웹 호스팅과 Flutter 스토어 제출은 이번 단계에서 수행하지 않았다.

### 백엔드 `synapse9983/aubl-backend:v20`

- 새 동기화 실행의 `createdAt` 초기화 누락을 수정했다.
- 워커의 내부 콜백 경로가 일반 사용자 인증 필터에서 차단되지 않도록 내부 서비스 인증 경로를 분리했다.
- 수집 요청의 초기 500 오류는 해소했으나, 후보 JSON 콜백에서 Jackson 2·3 타입 혼용으로 다시 500 오류가 발생하는 것이 확인됐다.

### 백엔드 `synapse9983/aubl-backend:v21`

- Spring Boot 4 환경에 맞춰 JSON 처리를 Jackson 3 `tools.jackson` API로 통일했다.
- 명시적인 Jackson 2 databind 의존성을 제거했다.
- `fields`, `fieldNames`, `findValuesAsText` 등 Jackson 2 전용 호출을 Jackson 3 대응 API로 교체했다.
- 후보 저장, 체크섬 검증, diff 생성까지 정상 진행되는 것을 확인했다.
- 전체 Gradle 테스트를 통과한 뒤 NAS에 배포했다.

### 백엔드 `synapse9983/aubl-backend:v22`

- 관리자가 `AUBL 항목에 연결(MAP_ENTITY)` 결정을 저장하면 해당 실행 항목뿐 아니라 `EXTERNAL_ENTITY_MAPPING` 원장에도 같은 트랜잭션에서 즉시 upsert하도록 변경했다.
- 팀·경기의 명시적 연결만 영구 매핑으로 저장한다. `UniquePlay 값 사용`, `AUBL 값 유지`와 같은 실행별 검수 판단은 기존 게시 정책을 유지한다.
- 게시 단계의 매핑 저장도 같은 upsert 함수를 재사용하므로 반복 실행과 재게시가 멱등하게 동작한다.
- Java 21 컨테이너에서 매핑 회귀 테스트와 백엔드 전체 Gradle 테스트를 통과했다.
- NAS 실행 환경에 맞춘 `linux/amd64` 이미지로 다시 빌드해 Docker Hub digest `sha256:d535941ff19775759cdd535f37db2c770d94ea900b6f48edd6f60df0c067db39`로 업로드했다.
- 2026-09-04 18:28 KST에 NAS의 `ix-aubl-backend-aubl-backend-1` 컨테이너를 v22로 교체했다. 컨테이너 실행 상태와 공개 `/api/seasons` HTTP 200 응답을 확인했다.

### 백엔드 `synapse9983/aubl-backend:v23`

- 최초 2026 시즌 리비전을 활성화할 때 `SEASON` 원장에 2026 행이 아직 없어 409가 발생하던 선행 조건 오류를 수정했다.
- 게시 전에 대상 시즌을 보장하고, 기존에 생성된 리비전을 직접 활성화하는 경로에서도 같은 방식으로 시즌을 생성한 뒤 행 잠금을 획득하도록 통일했다.
- UniquePlay 관리자 API의 `ResponseStatusException`을 RFC 9457 Problem Detail로 반환해 안전한 충돌 사유가 화면에 표시되도록 했다.
- 웹 API 클라이언트도 숫자 HTTP `status`를 업무 오류 코드로 오인하지 않고 `detail`을 우선 표시하도록 보강했다.
- Java 21·`linux/amd64` 환경에서 신규 시즌 생성 회귀 테스트, 활성화 잠금 테스트, Problem Detail 테스트와 백엔드 전체 Gradle 테스트를 통과했다.
- 프런트엔드는 lint, TypeScript typecheck와 프로덕션 빌드를 모두 통과했다.
- Docker Hub digest `sha256:4fc0717088f735a91ce5bef0d650238ee1f41643777d7b9c64c15ce6aee3c12f`로 업로드했다.
- 2026-09-04 18:51 KST에 NAS 백엔드 컨테이너를 v23으로 교체하고 공개 `/api/seasons` HTTP 200 응답을 확인했다.
- 18:52 KST에 기존 리비전 `ca075759-16ef-48c6-913f-a55453bda54f`의 활성화를 재시도해 성공했다. 2026 시즌은 `seasonId=12`로 생성됐고 해당 리비전이 현재 공개 상태가 됐다.

### 수집 워커 `synapse9983/aubl-uniqueplay-sync-worker:v4`

- UniquePlay 인증이 쿠키가 아니라 로컬 스토리지의 인증 항목을 함께 필요로 한다는 점을 반영했다.
- 세션 캡처 시 UniquePlay 도메인 쿠키와 `@accessToken`, `@userInfo`만 허용하고 Google 및 다른 도메인의 상태를 제거했다.
- React Native Web의 텍스트 노드 대신 실제 클릭 가능한 조상 요소를 선택하도록 탐색을 보강했다.
- 요약 카드와 상세 표의 중복 헤더 중 정확한 열 구성을 가진 표를 선택하도록 수정했다.
- A~H 조 선택, `조별일정확인`·`게임결과` 탭, 경기 카드 루트 탐색을 수정했다.
- 투수 `삼진` 열을 AUBL 표준 키 `탈삼진`으로 변환했다.
- 선수명과 팀명이 한 필드로 합쳐지던 고정 열 파싱을 수정하고 선수·팀 소속 검증을 추가했다.
- 결과적으로 선수·팀 관련 `PLAYER_TEAM` 차단 오류는 제거됐으나 경기 결과 목록의 일부만 수집하고 `콜드승`, `몰수게임`을 종료 경기로 보지 못해 `GAME_COUNT_MISMATCH`가 남았다.

### 수집 워커 `synapse9983/aubl-uniqueplay-sync-worker:v5`

- 스크롤 컨테이너를 화면 높이가 아니라 실제 스크롤 가능 거리(`scrollHeight - clientHeight`)가 가장 큰 요소로 선택하도록 수정했다.
- 목록이 이동 가능한 동안에는 행 수가 잠시 유지돼도 수집 종료로 판단하지 않고, 실제 목록 끝에서 3회 안정화될 때까지 계속 수집하도록 변경했다.
- `콜드승`, `몰수게임`, 기권·부전승 표현을 `COMPLETED`로 정규화했다.
- 워커 단계에서도 순위표 경기 수와 종료 경기 출전 수를 팀별로 대조하고, 불일치 시 조·팀·기대값·실제값을 포함한 차단 오류를 만든다.
- 전체 실데이터 미리보기에서 125경기(종료 117, 예정 8), A~H 40팀, 타자 846명, 투수 247명을 수집했다.
- 순위표 경기 수 불일치 0건, 워커 차단 오류 0건을 확인했다.
- Docker Hub digest `sha256:4ffccb674cc91deab9a4b29c8877193510d5fb3e624877f3b82f4cae09ae6692`로 업로드했다.
- 2026-09-04 18:04 KST에 NAS의 `aubl-uniqueplay-sync-worker` 컨테이너를 v5로 교체하고 실행 상태와 인증 세션 연결을 확인했다.

### 소스 검증 `2026.09.04.5`

- 워커·파서 소스 검증을 수행해 테스트 15/15를 통과했다.
- 이 검증 결과는 아직 Docker 이미지로 배포하지 않았다. 다음 단계에서 Docker worker/parser를 빌드한 뒤 관리자가 명시적으로 수동 동기화를 시작한다.

## 4. 주요 장애와 원인

| 증상 | 원인 | 해결 |
| --- | --- | --- |
| `/api/seasons` 및 동기화 API 530 | Cloudflare Tunnel이 NAS의 cloudflared에 연결되지 않음 | NAS 터널 복구 후 백엔드 접근 정상화 |
| 수집 시작 시 500 | 실행 생성 시각 누락, 내부 콜백 인증 경로 충돌 | 백엔드 v20 |
| 후보 콜백 500 | Spring Boot 4의 Jackson 3와 Jackson 2 타입 혼용 | 백엔드 v21 |
| Google 로그인 차단 | 자동화 브라우저로 판단된 로그인 환경 | 별도 일반 Chrome 프로필에서 로그인 후 CDP로 UniquePlay 상태만 캡처 |
| 대량 `PLAYER_TEAM` 오류 | 선수명과 팀명이 한 고정 셀 문자열로 합쳐짐 | 워커 v4 고정 열의 leaf text 분리 |
| 40건 `GAME_COUNT_MISMATCH` | 스크롤 컨테이너 오선택으로 종료 경기 6개만 수집 | 워커 v5의 최대 스크롤 거리 선택과 지연 목록 안정화 |
| 21건 `GAME_COUNT_MISMATCH` 잔존 | 수집된 `콜드승` 14건과 `몰수게임` 5건을 종료 상태로 분류하지 않음 | 워커 v5 상태 정규화 |
| v22 컨테이너 생성 시 `No such image` | 최초 v22 이미지를 로컬 기본 아키텍처인 arm64로 빌드해 amd64 NAS에서 사용할 수 없었음 | `docker build --platform linux/amd64`로 재빌드·재업로드한 뒤 NAS에서 이미지를 다시 받아 교체 |
| 리비전 활성화 시 409 | 게시기가 시즌을 만들 수 있음에도 활성화 서비스가 먼저 2026 시즌 존재를 요구함 | 백엔드 v23에서 게시·활성화 진입 시 시즌을 멱등 생성하고 실제 충돌 사유를 Problem Detail로 반환 |

## 5. 검증 결과

### 정적·단위 검증

- 워커 Node 테스트: 15개 통과
- ESLint: 워커 어댑터, 검증기와 관련 테스트 통과
- 웹 TypeScript typecheck: 통과
- 백엔드 Gradle 전체 테스트: 통과(v23 빌드 기준)
- 백엔드 v22 매핑 회귀 테스트 및 전체 Gradle 테스트: 통과(Java 21 컨테이너)
- 백엔드 v23 시즌 생성·활성화 잠금·오류 응답 회귀 테스트 및 전체 Gradle 테스트: 통과(Java 21, linux/amd64 컨테이너)
- 웹 lint, TypeScript typecheck, 프로덕션 빌드: 통과(v23 오류 응답 표시 보강 포함)

추가된 핵심 회귀 테스트는 다음과 같다.

- UniquePlay 도메인 인증 정보만 남기고 Google 쿠키를 제거한다.
- 가상 표는 행 수가 3회 안정화될 때까지 읽는다.
- 경기 목록은 스크롤이 가능한 동안 중간의 동일 샘플 때문에 조기 종료하지 않는다.
- 선수명이 비었거나 선수가 해당 조 팀에 속하지 않으면 차단한다.
- 순위표 경기 수와 종료 경기 출전 수가 다르면 차단한다.
- 콜드승·몰수게임은 종료 경기로 정규화한다.

### 2026 시즌 실데이터 미리보기

| 항목 | 결과 |
| --- | ---: |
| 조 | A~H, 8개 |
| 팀 | 조별 5팀, 총 40팀 |
| 경기 | 총 125경기 |
| 종료 경기 | 117경기 |
| 예정 경기 | 8경기 |
| 타자 기록 | 846명 |
| 투수 기록 | 247명 |
| 순위표 경기 수 불일치 | 0건 |
| 차단 오류 | 0건 |
| 진출 경계 동률 경고 | 3건(A·F·G조) |

동률 경고는 데이터 수집 실패가 아니라 회칙에 따른 관리자 판정 대상이므로 차단 오류와 구분한다.

v5 NAS 배포 후 관리자가 시작한 실행 `ef66a5d6-a7cb-4c35-a740-a393ebc8946e`도 총 1,266개 비교 항목(신규 1,226, 변경 없음 24, 팀 매핑 충돌 16)을 생성했다. 이전 v4 후보보다 경기 항목이 111개 늘어 전체 경기 수집 수정이 실제 운영 워커에 반영된 것을 확인했다.

v22 배포 후 이 실행의 16개 팀 충돌에 기존 AUBL 팀 ID 연결 결정을 다시 저장했다. 미해결 항목은 16건에서 0건으로 감소했고 서버 검증을 통과해 `게시 준비 완료` 상태가 됐다. 이 연결은 영구 매핑 원장에 저장됐다.

같은 실행에서 생성된 리비전은 첫 활성화 시 2026 시즌 선행 조건 때문에 409가 발생했다. v23 배포 후 새 수집이나 새 리비전 생성 없이 기존 리비전의 활성화를 다시 요청했고 정상 완료됐다. 현재 운영 화면에는 F·G조 진출 경계의 `TIE_PENDING` 팀이 각각 2개씩 남아 있으며, 이는 데이터 오류가 아니라 관리자 판정과 예선 종료를 기다리는 상태다.

### 2026-09-04 기준 공개 데이터·화면 상태 (과거 기록)

- 전체 경기 원장은 159건이다. 이 중 활성 UniquePlay 경기는 125건(종료 117건, 예정 8건)이며, 이전 레거시 경기는 34건이 보관(archived) 상태다.
- 현재 `reviewRequired`로 표시된 항목은 4건이며, 관리자 검토 대상으로 공개 활성화와 구분한다.
- 개인 기록의 현재 숫자 값은 `null`로 남아 있는 상태가 있다. 공개 UI는 불완전한 기록 행을 숨겨 유효하지 않은 숫자를 노출하지 않는다.
- 다음 단계는 Docker worker/parser를 배포하고, 배포 후 관리자가 버튼으로 수동 동기화를 시작하는 것이다. 자동 스케줄러는 도입하지 않는다.

### 2026-09-04 웹 배포 확인 (과거 기록)

- Firebase preview와 live는 프로젝트 `aubl-backup`에 배포했다.
- preview에서의 CORS 오류는 preview 도메인이 허용 목록에 없는 데 따른 예상 동작이다. live API 호출은 성공했다.
- 저장소에 남은 GitHub Actions 설정은 Firebase 프로젝트 `aubl-web-9a141`을 가리키는 오래된 값이므로 현재 `aubl-backup` 배포 확인 기준과 혼동하지 않는다.
- Flutter는 이번 범위에서 제외했으며 `flutter_app/pubspec.lock` 변경은 이번 문서 업데이트에 포함하지 않았다.

## 6. 관련 Git 커밋

| 커밋 | 내용 |
| --- | --- |
| `9d1436d` | 수동 UniquePlay 검수·게시 워크플로 도입 |
| `89fc8da` | 관리자 화면의 UniquePlay 인증 오류 안내 개선 |
| `4933d47` | 보안 Chrome 세션 캡처 기반 추가 |
| `ab715de` | UniquePlay 허용 인증 상태만 캡처하도록 보강 |
| `3a0af75` | 전체 경기·선수 기록 파싱, 지연 목록, 상태 정규화, 무결성 검증 수정 |
| `224f034` | 검증 실패·검토 완료 상태를 활성 실행으로 오인하지 않도록 관리자 재실행 판정 수정 |
| `4fed259` | UniquePlay 구현·배포·장애·검증 이력 문서화 |
| `3281a56` | 워커 v5 운영 배포와 백엔드 v22 영구 매핑 변경 이력·재현 패치 기록 |
| `430e96c` | 시즌 활성화 전에 2026 시즌 원장을 보장하도록 수정 |
| `d83137f` | 잘못된 선수 기록 스냅샷 거부 |
| `18088b1` | 대체된 레거시 경기 원장 보관 처리 |

## 7. 배포와 롤백 절차

1. 워커 테스트와 ESLint를 실행한다.
2. `linux/amd64` 워커 이미지를 새 불변 태그로 빌드한다.
3. Docker Hub에 업로드하고 manifest digest를 기록한다.
4. Portainer에서 워커 컨테이너 이미지를 새 태그로 변경하고 재생성한다.
5. `/health`와 관리자 화면의 `세션 다시 확인`에서 연결 상태를 확인한다.
6. 관리자가 버튼을 눌러 새 수동 수집을 시작한다.
7. 후보 수치, 차단 오류, 충돌과 체크섬을 검토한다.
8. 검증이 통과해도 리비전 생성과 활성화는 별도 관리자 확인 후 수행한다.

문제가 발생하면 Portainer에서 직전 이미지 태그로 되돌려 재생성한다. 후보 리비전은 활성화 전까지 공개 데이터에 영향을 주지 않는다. 이미 활성화한 경우에는 이전 게시 리비전을 다시 활성화해 공개 projection을 복구한다.

## 8. 운영상 남은 확인 사항

- 다음에 관리자가 명시적으로 시작하는 새 수동 실행에서 이번에 저장한 16개 팀 매핑이 자동으로 재사용되는지 최종 확인한다.
- 활성화된 공개 리비전의 F·G조 진출 경계 동률은 회칙에 따라 관리자가 판정한다. 현재 예정 경기 8개가 모두 종료 또는 취소되기 전에는 최종 확정을 실행하지 않는다.
- 동률 팀의 으뜸권·버금권·진출권 밖 배정은 운영 판단이므로 별도 지시 없이 자동 결정하지 않는다.
- 자동 스케줄러는 도입하지 않는다. 향후 필요할 때도 현재 수집 파이프라인 앞에 별도 승인된 스케줄러만 추가한다.
- 임시 로컬 세션 파일과 전용 Chrome 프로필은 배포 검증 완료 후 정확한 경로를 확인해 폐기한다.
