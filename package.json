{
  "name": "aubl-website",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "node build.js",
    "build": "node build.js",
    "lint": "echo 'Linting is not configured for this demo build.'",
    "preview": "node build.js && npx --yes http-server dist --port 4173"
  },
  "dependencies": {},
  "devDependencies": {}
}
